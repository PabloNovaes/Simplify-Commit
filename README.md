# Simplify Commit

**simplify-commit**
simplifica o processo de fazer commits no Git, automaizando a realização de 3 comandos:

1. Adiciona todos os arquivos modificados.
2. Solicita uma mensagem de commit.
3. Realiza o commit e faz o push para a branch principal (geralmente `main` ou `master`).

### 🚀 Como Funciona

Com **simplify-commit**, você pode simplificar seu fluxo de trabalho de Git. Basta executar um único comando para adicionar, nomear e enviar suas mudanças para a branch principal sem precisar digitar várias linhas no terminal.

### 📦 Instalação

Se você não tem o pacote instalado globalmente, pode fazer isso rodando o comando:

```bash
npm install -g simplify-commit
```
